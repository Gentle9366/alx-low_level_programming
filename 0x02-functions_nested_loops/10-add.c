#include "main.h"
#include <stdio.h>

/**
 *add - adds two integers and returns the result
 *@i: the int to print
 *@k: the int to print
 *Return: 0 if exited properly
 */

int add(int i, int k)
{
return (i + k);
}
