#include <stdio.h>

/**
 * main - code that prints the alphabet in lowercase, followed by a new line
 * Return: 0 if exited properly
 */
int main(void)
{
char alphabet;
for (alphabet = 'a'; alphabet <= 'z'; alphabet++)
{
putchar('\n');
return (0);
